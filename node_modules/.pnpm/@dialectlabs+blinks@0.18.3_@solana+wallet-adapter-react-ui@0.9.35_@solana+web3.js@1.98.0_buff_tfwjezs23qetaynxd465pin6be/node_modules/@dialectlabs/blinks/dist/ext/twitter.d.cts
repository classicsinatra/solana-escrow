import { ActionSupportStrategy, ActionAdapter, ActionCallbacksConfig, SecurityLevel } from '@dialectlabs/blinks-core';

type ObserverSecurityLevel = SecurityLevel;
interface ObserverOptions {
    securityLevel: ObserverSecurityLevel | Record<'websites' | 'interstitials' | 'actions', ObserverSecurityLevel>;
    supportStrategy: ActionSupportStrategy;
}
declare function setupTwitterObserver(config: ActionAdapter, callbacks?: Partial<ActionCallbacksConfig>, options?: Partial<ObserverOptions>): void;

export { type ObserverOptions, setupTwitterObserver };
