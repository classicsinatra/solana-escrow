import { ActionConfig } from '@dialectlabs/blinks-core';
import { Connection } from '@solana/web3.js';

/**
 * Hook to create an action adapter using solana's wallet adapter.
 *
 * Be sure to call `action.setAdapter` with the to update the adapter, every time the instance updates.
 *
 * @param rpcUrlOrConnection
 * @see {Action}
 */
declare function useActionSolanaWalletAdapter(rpcUrlOrConnection: string | Connection): {
    adapter: ActionConfig;
};

export { useActionSolanaWalletAdapter };
